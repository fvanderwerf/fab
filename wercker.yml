box: fsdvanderwerf/fedora-build:latest

build:
  steps:
      - script:
          code: |
              echo hello
              mkdir m4
              autoreconf -i
              ./configure
              make
              make check

