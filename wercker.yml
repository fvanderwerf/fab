box: fsdvanderwerf/fedora-build

build:
    steps:
        - script:
            - name: build fab
            - code: |
                mkdir m4 && autoreconf -i && ./configure && make && make check
